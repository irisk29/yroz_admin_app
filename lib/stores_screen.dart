import 'package:flutter/material.dart';

class StoresScreen extends StatelessWidget {
  static const routeName = '/stores';

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hello Stores"),
    );
  }
}
